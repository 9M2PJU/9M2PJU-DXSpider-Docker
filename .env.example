# DXSpider Docker Configuration
# Copy this file to .env and customize for your station
#
# cp .env.example .env
# nano .env

# =============================================================================
# DXSpider Basic Configuration
# =============================================================================

# Your cluster node callsign (with SSID, e.g., -10)
CLUSTER_CALLSIGN=YOURCALL-10

# DXSpider branch to use: "mojo" (recommended) or "master"
CLUSTER_DXSPIDER_BRANCH=mojo

# =============================================================================
# Sysop Information
# =============================================================================

# Your name (displayed to users)
CLUSTER_SYSOP_NAME=YourName

# Your personal callsign (without SSID)
CLUSTER_SYSOP_CALLSIGN=YOURCALL

# Sysop password (leave empty to disable, or set a strong password)
CLUSTER_SYSOP_PASSWORD=

# =============================================================================
# Station Location
# =============================================================================

# Latitude in decimal degrees (positive = North, negative = South)
CLUSTER_LATITUDE=+0.0

# Longitude in decimal degrees (positive = East, negative = West)
CLUSTER_LONGITUDE=+0.0

# Maidenhead grid locator (6 characters)
CLUSTER_LOCATOR=AA00AA

# QTH description (city, country)
CLUSTER_QTH=Your City, Country

# =============================================================================
# Contact Information
# =============================================================================

# Sysop email address
CLUSTER_SYSOP_EMAIL=your@email.com

# BBS address (optional)
CLUSTER_SYSOP_BBS_ADDRESS=

# =============================================================================
# Network Configuration
# =============================================================================

# Hostname for your DXSpider node (for partner connections)
CLUSTER_DX_HOSTNAME=your.hostname.com

# Telnet port for DX cluster clients (default: 7300)
CLUSTER_PORT=7300

# Web console port for sysop access via browser (default: 8050)
CLUSTER_SYSOP_PORT=8050

# =============================================================================
# Database Configuration (Optional)
# =============================================================================
# Leave empty to use file-based storage, or configure for MariaDB/MySQL

# Database username
CLUSTER_DB_USER=sysop

# Database password (CHANGE THIS! Use a strong, unique password)
CLUSTER_DB_PASS=CHANGE_ME_STRONG_PASSWORD

# Database root password (only needed if using included MariaDB)
CLUSTER_DB_ROOT_PWD=

# Database name
CLUSTER_DB_NAME=

# Database hostname (use 'mariadb' if using docker-compose MariaDB service)
CLUSTER_DB_HOSTNAME=

# Database port (default: 3306)
CLUSTER_DB_PORT=

# =============================================================================
# Phase 4: Dashboard Configuration (Optional)
# =============================================================================

# Dashboard web interface port (default: 8080)
DASHBOARD_PORT=8080

# CORS origin for dashboard (leave empty to disable CORS, set for reverse proxy)
# Example: http://localhost:3000 or https://yourdomain.com
DASHBOARD_CORS_ORIGIN=

# Maximum number of spots to display in dashboard (default: 100)
DASHBOARD_MAX_SPOTS=100

# =============================================================================
# Phase 4: Metrics and Monitoring (Optional)
# =============================================================================

# Grafana admin username (default: admin)
GF_ADMIN_USER=admin

# Grafana admin password (REQUIRED - change this!)
GF_ADMIN_PASSWORD=changeme

# Metrics server port for Prometheus (default: 9100)
CLUSTER_METRICS_PORT=9100
